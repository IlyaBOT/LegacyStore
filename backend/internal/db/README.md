# Database package

PostgreSQL connection and query helper package.

The current backend uses direct `database/sql` access from the catalog store.
Shared database helpers can be moved here when more packages need them.
